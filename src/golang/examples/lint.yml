description: Linting a Go repo

usage:
  version: 2.1

  orbs:
    golang: moorara/golang@0.0.0

  workflows:
    lint:
      jobs:
        - golang/lint:
            circle-token: CIRCLECI_API_TOKEN
